name: debounce_hook
description: A Flutter package that helps debouncing.
version: 0.0.1
homepage: https://github.com/Abduraimbek/debounce_hook

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
